#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../tsx/dist/cli.mjs" "$@"
else
  exec node  "$basedir/../../../tsx/dist/cli.mjs" "$@"
fi
# cmd-shim-target=/data/data/com.termux/files/home/desi4k/node_modules/.pnpm/vite@7.3.5_@types+node@25.9.3_jiti@2.7.0_lightningcss@1.32.0_tsx@4.22.4/node_modules/tsx/dist/cli.mjs
