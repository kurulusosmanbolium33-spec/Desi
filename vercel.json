{
  "buildCommand": "pnpm --filter @workspace/desi4k run build",
  "outputDirectory": "artifacts/desi4k/dist/public",
  "installCommand": "corepack enable && corepack prepare pnpm@10.26.1 --activate && pnpm install",
  "framework": null,
  "env": {
    "BASE_PATH": "/"
  },
  "build": {
    "env": {
      "BASE_PATH": "/"
    }
  }
}
